import express from 'express';

const router = express.Router();

// Placeholder for task-related routes
// TODO: define routes like GET /tasks, POST /tasks, etc., here in the future

export default router;